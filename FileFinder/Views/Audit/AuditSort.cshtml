@model FileFinder.Models.Audit
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<h2>Audit Groups</h2>


<h4>First Quarter</h4>
<ol>
    @foreach (File f in Model.FirstQuarter)
    {
        <li> @f.Room.Name: @f.Consumer.FullName() </li>
    }
</ol>

    <h4>Second Quarter</h4>
    <ol>
        @foreach (File f in Model.SecondQuarter)
        {
            <li> @f.Room.Name: @f.Consumer.FullName() </li>
        }
    </ol>

    <h4>Third Quarter</h4>
    <ol>
        @foreach (File f in Model.ThirdQuarter)
        {
            <li> @f.Room.Name: @f.Consumer.FullName() </li>
        }
    </ol>

    <h4>Fourth Quarter</h4>
    <ol>
        @foreach (File f in Model.FourthQuarter)
        {
            <li> @f.Room.Name: @f.Consumer.FullName() </li>
        }
    </ol>
